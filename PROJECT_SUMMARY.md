# 电力系统文献雷达：项目摘要

本项目是一个仅在本机运行的文献检索、筛选、去重、解读与邮件推送工具，面向电力系统、综合能源、优化调度及相关方向。

## 当前能力

- 从 OpenAlex、Crossref、arXiv、Semantic Scholar、IEEE Xplore API、Elsevier/Scopus API、RSS 和手工导出文件获取文献元数据；
- 以关键词、期刊范围、文献类型和相关度进行筛选；
- 通过 DOI、来源 ID 和规范化标题避免同一推送方案重复发送；
- 使用 DeepSeek 生成中文摘要翻译、逐篇概括和当次总体简报；
- 邮件正文只发送总体简报，完整内容位于交互式 HTML 附件；
- 期刊、预印本和会议论文拥有独立的日/周推送计划；
- 本机可视化控制台支持方案、关键词、邮箱、来源、计划、API 与 SMTP 的增删改查。

## 多方案设计

控制台不再固定为 Basic/Full 两套。首次升级时保留这两套兼容方案，之后可以新增、复制、重命名和删除推送方案。

每个方案独立拥有：

- 配置与关键词；
- 收件人和本机凭据；
- 三类文献计划任务；
- 已发送文献状态；
- 日志和报告输出目录。

最大支持 20 个方案，即最多 60 个 Windows 计划任务。此限制适合个人电脑和小型课题组；更大规模应改用服务器队列和数据库。

## 安全边界

- API Key、SMTP 授权码、真实邮箱、运行日志、状态和报告不进入 Git；
- 公开仓库只提供 `radar.env.example.ps1` 占位模板；
- 控制台仅监听 `127.0.0.1`；
- 删除方案时先停用计划任务，并把文件移动到本机回收目录；
- 商业数据库只使用官方 API 或用户手工导出的文件，不自动抓取登录页面。

## 使用与维护

完整安装、API 申请、SMTP、多人方案、计划任务、去重机制和故障排查说明见 [README.md](README.md)。

